〇stackに関して、overflowを想定していない。
→parser内のstackをいじる箇所は全部overflow判定をしなければならない。
→processor内も同様
→processor内では万が一にもアクセスが小数になることを避ける。

命令が存在しないメモリをたたいたらエラーだったり、
run、stepに関して実行可能状態と実行状態を分ける。

アクセス許可のないレジスタへのアクセス禁止
→基本的にprocessorを修正すればいいかな？

ラベルの存在有無の確認はprocessorでやってるが、
parserでやった方がいいかな？


〇字句解析、構文解析のエラーを表示する。

stackやprogramのハイライトされた箇所にスクロールする仕様

コード自体へのハイライト

〇runすると一気にハイライトされる問題

〇細かい命令の追加

アドレス空間を1byte単位にするバージョンも検討中

sp, pcを横に配置

reinitializeと再実行ボタンあたりを追加